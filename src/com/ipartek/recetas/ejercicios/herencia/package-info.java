/**
 * Ejercicios sobre herencia - capitulo 8
 *
 *
 * @author Curso
 *
 *         <h2>Interfaces</h2> Las interfaces nunca pueden tener codigo
 *         implementado. Pueden existir interfaces vacias. Soportan herencia
 *         multiple al contrario que las clases. Pueden contener variables, pero
 *         seran finales. Las interfaces no son instanciables (no se pueden
 *         crear objetos)
 * 
 *         <h2>Clases abstractas</h2> Clase abstracta porque tiene un metodo
 *         abstracto "dibujar". No se pueden instanciar al igual que las
 *         interfaces. Este metodo sera implementado por sus clases hijas o no.
 *         Si una clase hija no implementa el metodo abstracto, debe ser una
 *         clase abstracta.
 * 
 */

package com.ipartek.recetas.ejercicios.herencia;